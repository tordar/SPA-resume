This will be a single page application, using Express and no frameworks
An SPA requires no page refreshing, making it fast
It primarily uses either AJAX or Fetch requests to update the page

It will be deployed via AWS to get experience in a new type of deployment

